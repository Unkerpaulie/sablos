[Unit]
Description=sablos Django site (gunicorn)
After=network.target

[Service]
Type=notify
User=sablos
Group=sablos
WorkingDirectory=/var/www/sablos_root/sablos
EnvironmentFile=/var/www/sablos_root/sablos/.env
ExecStart=/var/www/sablos_root/sablos/.venv/bin/gunicorn \
    --workers 3 \
    --bind unix:/run/sablos/sablos.sock \
    --access-logfile - \
    --error-logfile - \
    config.wsgi:application
Restart=on-failure
RestartSec=5
RuntimeDirectory=sablos
RuntimeDirectoryMode=0755

[Install]
WantedBy=multi-user.target
